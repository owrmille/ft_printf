#include "ft_printf.h"
# include <stdio.h>
// to compile and run:
// 1. make
// 2. cc -Wall -Wextra -Werror -o test test.c -L. -lftprintf
// (where test.c can be any file with tests)

int	main(void)
{
	ft_printf();
	return (0);
}
